version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu

 hooks:
   BeforeInstall:
    - location: ./beforeInstall.sh
      timeout: 180
      runas: centos
   AfterInstall:
    - location: ./afterInstall.sh
      timeout: 180
      runas: centos
